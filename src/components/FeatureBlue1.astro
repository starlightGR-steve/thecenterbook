<section class="section bg-blue">
  <div class="section-inner">
    <span class="eyebrow" style="color:var(--primary)">Scheduling + Staff</span>
    <div class="feature-split reverse">
      <div class="feat-text">
        <span class="script script-xl" style="color:var(--primary)">Schedules, Shifts, and Time</span>
        <h2 class="headline">Student schedules, staff shifts, and time tracking. One place.</h2>
        <p class="subhead">Per-student, per-day schedules with weekly overrides. Staff shift management with conflict detection. Timeclock with CSV export. Booking calendar for progress meetings.</p>
        <div class="feat-points">
          <div class="feat-point"><div class="feat-point-icon teal"><i data-lucide="calendar" style="width:22px;height:22px;color:var(--secondary-text)"></i></div><span><strong style="color:var(--text)">Student scheduling</strong><br>Per-day recurring schedules with per-week overrides</span></div>
          <div class="feat-point"><div class="feat-point-icon blue"><i data-lucide="users" style="width:22px;height:22px;color:var(--primary)"></i></div><span><strong style="color:var(--text)">Staff shifts + coverage grid</strong><br>15-min precision blocks with conflict detection</span></div>
          <div class="feat-point"><div class="feat-point-icon orange"><i data-lucide="clock" style="width:22px;height:22px;color:var(--accent-text)"></i></div><span><strong style="color:var(--text)">Staff timeclock</strong><br>PIN/barcode entry, overtime split, CSV export for your payroll provider</span></div>
          <div class="feat-point"><div class="feat-point-icon tertiary"><i data-lucide="calendar" style="width:22px;height:22px;color:var(--tertiary)"></i></div><span><strong style="color:var(--text)">Booking calendar</strong><br>Progress meetings with create, reschedule, and cancel</span></div>
        </div>
        <div><a href="#" class="btn btn-founding" data-cta="See It Live">See It Live</a></div>
      </div>
      <div class="feat-visual">
        <div class="blob" style="background:radial-gradient(circle,rgba(53,92,170,.16),transparent 68%);width:124%;height:120%;top:-10%;left:-12%"></div>
        <div class="shot"><img src="/images/img-06.png" alt="Staff coverage grid"></div>
        <div class="float-el" style="bottom:30px;right:-15px"><i data-lucide="trending-up" style="width:14px;height:14px;color:var(--secondary-text)"></i> Capacity utilization</div>
      </div>
    </div>
  </div>
</section>

<!-- ==================== FEATURE 5: CURRICULUM ==================== -->
